import { BootstrapInputDirective } from './bootstrap-input.directive';

describe('BootstrapInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapInputDirective();
    expect(directive).toBeTruthy();
  });
});
